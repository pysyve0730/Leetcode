char str[100];
int length = strlen(str);
char * toHex(int num){

    while(num!=0){
        if(num&1==1){
            str[length--]='1';

        }else{
            str[length--]='0';
        }
        num = num>>1;

    }

   printf("%s\n",str);
   return str;

}
