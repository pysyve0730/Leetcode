int i = 0;

char * toHex(int num){
    if(num>0){
        int tnp = num&1;
        int group = 0;
        int len = 0;
        while((tmp==1){
            int base = 2;
            group+=tmp* pow(base,i++);
            len++;
            num = num>>1;

            if(len==4){
                group = 0;

            }
        }
    }else{





    }


}
