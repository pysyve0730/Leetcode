#include <stdio.h>
#include <stdlib.h>
int singleNumber(int* nums, int numsSize) {
	int i = 0, j = 0, result=0;
	
	while (i < numsSize) {
		result ^= *(nums + i);
	    i++;
	}
	printf("%d", result);
	return result;



}
