char* reverseString(char* s, int sSize) {
    int i = 0;
    char temp;
    while (i < sSize/2) { //中間字元不更動，只有左右兩側互換
        temp = *(s + i);
        *(s + i) = *(s + sSize - 1 - i);
        *(s + sSize - 1 - i) = temp;
        i++;


    }
    return s;





}
